{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Output Schema for aggregate Operation",
  "description": "Output schema for the aggregate operation in the connector.",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The main response payload from the aggregate operation.",
      "properties": {
        "count": {
          "type": "integer",
          "description": "The total number of records found."
        },
        "data": {
          "type": "array",
          "description": "An array of objects representing the data returned from the aggregate operation."
        }
      }
    }
  },
  "required": [
    "payload"
  ],
  "additionalProperties": false
}
